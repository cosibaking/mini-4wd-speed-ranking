.follow-item {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.follow-item__avatar {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: var(--color-border);
}

.follow-item__name {
  font-size: 30rpx;
}
